package fundamentals;

public class Logic {

    public static void main(String[] args) {

        boolean a = true;
        boolean b = false;

        // Logical AND
        System.out.println(a && b);

        // Logical OR
        System.out.println(a || b);

        // Logical NOT
        System.out.println(!a);

        /*
         * && = both true
         * || = at least one true (one sided love)
         */
    }
}