package notes;

public class coverage {
//ang hirap mo mahalin programming :<
    /*
     * Chapter 5 Summary
     * - A class can contain fields, methods, constructors, and nested types.
     * - An object is an instance of a class.
     * - Object references store addresses, not the actual object itself.
     * - this refers to the current object.
     * - Scope can be instance, static, local, or block level.
     * - Objects become eligible for garbage collection when no reachable references remain.
     * - //may idadagdag pa hoooo
     */
}