package encapsulation;

public class Notes {

    /*
     * ENCAPSULATION RULES
     *
     * private
     * - same class lang accessible
     *
     * package-private
     * - same package lang accessible
     *
     * protected
     * - same package
     * - subclasses
     *
     * public
     * - accessible everywhere
     *
     * IMMUTABLE CLASS RULES
     *
     * - class should be final
     * - fields should be private final
     * - no setters
     * - values set through constructor
     *
     * IMPORTANT
     *
     * Encapsulation =
     * pagtatago ng internal data
     * gamit private fields
     * at controlled access methods
     *
     */
}